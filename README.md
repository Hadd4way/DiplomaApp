# DiplomApp

Minimal Electron + Vite + React + TypeScript desktop app.

## Dev

```bash
npm install
npm run dev
```

By default, the renderer AI requests use `https://diplomaapp-production.up.railway.app`.
To override the backend URL for a local server, set `VITE_AI_BACKEND_URL`.

## Build

```bash
npm run build
```
